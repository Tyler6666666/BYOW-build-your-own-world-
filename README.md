# BYOW - Build Your Own World

Windows quick start:

1. Double-click `BYOW.bat`.

The launcher compiles `BYOW/src` into `BYOW/out/game` and starts the game.

The required Java library is included in `library-sp26`. If you want to use a different copy, set
`BYOW_LIBRARY` to that folder before running the launcher.
